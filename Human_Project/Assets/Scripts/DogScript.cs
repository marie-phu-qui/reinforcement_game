using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogScript : MonoBehaviour
{

    /* On collision follows human - after 3 correctly sorted trash in
     * a row - dog can pick up its own trash (1 extra point of carrying)
     * at the first mistake the dog becomes rogue and runs away 
     wait for another dog to spawn */


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
